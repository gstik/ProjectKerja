version: '3.8'
services:
  ctf-web:
    build: .
    ports:
      - "3075:3075"
    restart: always
    environment:
      - NODE_ENV=production
    volumes:
      - /opt/admin/logs:/var/log/app_logs # Mapped for potential shared logging